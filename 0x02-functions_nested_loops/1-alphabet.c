#include "main.h"

/**
 * print _alphabet - prints the alpha in lowercase
 */
* Description : print all function prototype used in this project
void print_alphabet(void)
{
	char la;

	for (la = 'a'; la <= 'z'; la++)
		_putchar(la);

	_putchar('\n');
}
